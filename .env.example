SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Deploy button — used by plugins/deployButton/DeployTool.tsx.
# SANITY_STUDIO_DEPLOY_HOOK_SECRET must match DEPLOY_HOOK_SECRET on the
# Netlify side (the function validates this on the wire).
SANITY_STUDIO_DEPLOY_FUNCTION_URL=
SANITY_STUDIO_DEPLOY_HOOK_SECRET=
